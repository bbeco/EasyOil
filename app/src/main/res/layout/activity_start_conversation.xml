<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context="com.example.andrea.tabsactionbar.chat.StartConversationActivity">

    <RelativeLayout
        android:id="@+id/search_result_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_below="@+id/toolbar"
        android:visibility="gone">

        <TextView
            android:id="@+id/search_result_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:gravity="center"
            android:textStyle="bold"
            android:text="@string/search_results"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/old_conversations"
            android:gravity="center"
            android:textStyle="bold"
            android:layout_below="@+id/search_result_list"
            android:layout_alignParentBottom="true"/>

        <ListView
            android:id="@+id/search_result_list"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_below="@+id/search_result_title"/>

    </RelativeLayout>

    <ListView android:id="@+id/conversation_list"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        android:layout_below="@+id/search_result_container"/>

</RelativeLayout>
